[package]
name = "osm"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
toml = "0.8"
semver = { version = "1", features = ["serde"] }
zstd = "0.13"
tar = "0.4"

[dev-dependencies]
